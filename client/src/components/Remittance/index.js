import React from 'react'
import RemittanceTable from '../Table/RemittanceTable'

const Remittance = ({ remittance }) => {
    return (
        <RemittanceTable remittance={remittance} />
    )
}

export default Remittance
